repeat wait() until game:IsLoaded()
local Venyx = loadstring(game:HttpGet("https://raw.githubusercontent.com/Stefanuk12/Venyx-UI-Library/main/source2.lua"))()
local UI = Venyx.new({
  title = "Animal Simulator - jokereddy"
})

game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer("jokereddy script loaded" ,"all")

local Themes = {
  Background = Color3.fromRGB(24, 24, 24),
  Glow = Color3.fromRGB(0, 0, 0),
  Accent = Color3.fromRGB(10, 10, 10),
  LightContrast = Color3.fromRGB(20, 20, 20),
  DarkContrast = Color3.fromRGB(14, 14, 14),  
  TextColor = Color3.fromRGB(255, 255, 255)
}

local Main = UI:addPage({
    title = "Main",
})
local Settings = UI:addPage({
    title = "Settings",
})
local Extras = UI:addPage({
    title = "Extras",
})

--[[ local generalDiv = Main:addsection({
    title = "General",
}) ]]--
local collectingDiv = Main:addSection({
    title = "Collecting"
})
local Colours = Settings:addSection({
  title = "Colours"
})
local More = Extras:addSection({
  title = "More"
})

local function findPlr(name)
    for _,v in pairs(game:GetService("Players"):GetPlayers()) do
        if string.find(v.Name, name) then
            return v
        end
    end
    return nil
end


local function damageplayer(player)
for i,p in pairs(game.Workspace:GetChildren()) do
if p.Name == player then
print("Damaging " .. player)
game:GetService("ReplicatedStorage").jdskhfsIIIllliiIIIdchgdIiIIIlIlIli:FireServer(p.Humanoid,1) -- the last arguement is not damage don't bother changin it
print("Damaged " .. player)
end
end
end


local chestCollect = false
collectingDiv:addToggle({
    title = "Collect Chests", -- Set the title
    toggled = nil,
    callback = function(value) -- Calls back on a function with a value
        chestCollect = value
        while wait() and chestCollect do
local args = {
                    [1] = workspace.Eggs.Egg7
                }

                game:GetService("ReplicatedStorage").EggEvent:FireServer(unpack(args))
                game:GetService("ReplicatedStorage").EggEvent:FireServer(unpack(args))

                local args = {
                    [1] = workspace.Treasures.Treasure1
                }

                game:GetService("ReplicatedStorage").TreasureEvent:FireServer(unpack(args))
                game:GetService("ReplicatedStorage").TreasureEvent:FireServer(unpack(args))

		if game:GetService("Workspace")[game.Players.LocalPlayer.Name]:FindFirstChild("justFound") ~= nil then
			game:GetService("Workspace")[game.Players.LocalPlayer.Name].justFound:Destroy()
   		end

		game:GetService("ReplicatedStorage").jdskhfsIIIllliiIIIdchgdIiIIIlIlIli:FireServer(game:GetService("Workspace").OLDMAP.pleaseno["Dummy2"].Humanoid,5)
end
    end})
            
for theme, color in pairs(Themes) do
    Colours:addColorPicker({
        title = theme,
        default = color,
        callback = function(color3)
            UI:setTheme({
                theme = theme,
                color3 = color3
            })
        end
    })
end

UI:SelectPage({
    page = UI.pages[1],
    toggle = true
})
